/*
import { BrowserRouter } from "react-router-dom"
import ReactRouterDemo from './reactRouterDemo'

const ReactRouterApp = () => {
  // it is not working but shows the priciple
  // obviously the <BrwowserRouter> has to wrap <App /> in indes.tsx
  // and the <Routes> are returned in App.tsx
  return (
    <BrowserRouter>
      <ReactRouterDemo />
    </BrowserRouter>
  )
}
export default ReactRouterApp
*/